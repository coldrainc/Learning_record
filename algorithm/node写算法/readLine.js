let readline = require('readline');
const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
})

// 单行输入
rl.on('line', function(line) { // line输入

})
let lines = [];
// 多行输入
rl.on('line', function(line) {
  lines.push(line);
  if (lines.length === 2) { // 等于需要输入的行时，处理

  }
})
let inputArr = [];
// 多行输入
rl.on('line', function(line) {
  inputArr.push(line);
  let inputs = line.split(' ');
  let nline = 
})
